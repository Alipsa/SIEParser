pluginManagement {
  repositories {
    if (gradle.startParameter.projectProperties.containsKey("useMavenLocal")) {
      // Opt-in local plugin resolution: ./gradlew -PuseMavenLocal <task>
      mavenLocal()
    }
    mavenCentral()
    gradlePluginPortal()
  }
}

rootProject.name = 'SIEParser'
